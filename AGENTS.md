# 知练项目协作说明（AGENTS.md）

> 最后更新：2026-09-04
> 适用范围：整个仓库  
> 项目仓库：`https://github.com/qq808shd/zhilian-mini-program.git`

本文件是“知练”微信小程序的长期项目上下文。它保存已经确认的产品方向、数据规则、交互要求、技术约束和提交流程，供 Codex 在不同电脑、不同任务中继续工作。这里记录的是对话结论，不是逐字聊天备份。

## 0. 每次工作都必须遵守

1. 开始修改前先完整阅读本文件，再查看 `git status`、相关代码和最新提交；不得仅凭旧对话或猜测操作。
2. 用户的最新明确要求优先于本文件。若新要求改变了长期规则，实施代码的同时更新本文件。
3. **每次准备提交并推送任何项目变更时，都必须同步更新本文件**：
   - 更新顶部“最后更新”日期；
   - 在“变更记录”中写明本次长期有效的需求、数据基线或实现变化；
   - 将 `AGENTS.md` 与本次代码、数据、界面或文档一起加入同一个提交；
   - 提交前用 `git status` 确认 `AGENTS.md` 已被跟踪，不能漏交。
4. 不把逐字聊天、隐私、账号凭证、令牌、本机临时路径或 Codex 私有记忆写进本文件。只保留以后开发确实需要的结论。
5. 如果只是一次性排查，且没有任何文件需要提交，不必为了制造记录而单独修改本文件；一旦要提交项目变更，就必须按第 3 条更新。
6. 保留用户已有的未提交改动，不覆盖、不回滚无关文件。删除、重排或替换大量数据前先确认影响。

## 1. 项目定位与范围

- 产品名：**知练**。
- 形态：原生微信小程序，当前 AppID 为 `wx01e13d3df486356c`。
- 用户：当前由本人及身边朋友使用，内容聚焦公考等需要记忆和客观题训练的知识。
- 核心闭环：**分类学习 → 分类考试 → 错题统计 → 针对性复习/再考**。
- 当前不做：社区、评论、关注、排行榜、社交分享体系、每日打卡或强制每日学习任务。
- 内容可重复学习，已完成一组不代表该组被锁定。
- GitHub 用于保存代码、整理后的学习资料和项目说明；它不是线上数据库，也不会自动保存 Codex 的完整对话上下文。本文件是跨电脑延续项目约束的主要入口。

## 2. 当前运行和架构基线

- 项目根目录直接导入微信开发者工具，`miniprogramRoot` 为 `miniprogram/`。
- 采用“**题库本地 + 用户数据云端 + 本地优先同步**”架构：学习资料仍随小程序打包，学习进度、答题统计和错题状态先写微信本地存储，再批量同步云端。
- 云端只保存微信用户标识、学习进度、答题统计、错题状态和同步元数据；不搬迁 888 条成语等静态题库。
- 小程序通过 `wx.login` 获取一次性 code；服务端用 AppID、AppSecret 换取 OpenID，并返回自己的签名会话。不得自建用户名/密码，也不主动收集昵称、头像、手机号。
- 自建服务端位于 `server/`：Node.js 24、内置 HTTP/加密/SQLite，无第三方运行依赖，默认监听 `127.0.0.1:8787`，由 Nginx 提供公网 HTTPS。
- 当前生产环境为阿里云北京地域轻量应用服务器（Ubuntu 24.04），已通过本机 SSH 固定别名 `zhilian-aliyun` 接入。知练部署目录为 `/opt/zhilian-mini-program`，运行用户为 `zhilian`，数据库目录为 `/var/lib/zhilian-api`；使用项目内隔离 Node.js 24.19，不替换系统 Node。Nginx 与 `zhilian-api` 已启用并运行，公网入口为 `https://api.dabaommz.cloud`。
- 服务端同步按唯一答题事件幂等处理，网络重试不重复累计；首次连接时把已有本机记录作为基线导入。
- 云端不可用时不能阻断学习；待同步事件必须持久保留在本地，恢复网络后自动重试。
- **生产连接已启用**：`miniprogram/config/cloud.js` 中 `enabled: true`，`baseUrl` 为 `https://api.dabaommz.cloud`。域名已备案并配置 HTTPS，且已加入微信 `request` 合法域名。
- 当前阿里云个人测试证书有效期截至 2026-12-01 23:59:59 UTC（北京时间 2026-12-02 07:59:59），到期前必须重新申请、替换证书并平滑重载 Nginx。
- 不使用 `wx.cloud`。启用后由 `wx.request` 访问自建阿里云服务器。
- AppSecret、`SESSION_SECRET`、生产 `.env`、SQLite 数据库和备份只能存在服务器，禁止进入小程序代码或 GitHub。
- `project.private.config.json` 是每台电脑的私有配置，已被 `.gitignore` 忽略；换电脑后由微信开发者工具重新生成。
- 小程序根目录没有 `package.json`；服务端有独立的 `server/package.json`，测试使用 Node 内置测试器。

## 3. 信息架构和核心功能

### 3.1 学习

- 入口按“模块 → 分类 → 分组”组织，不把所有内容铺成长列表。
- 当前模块包括：言语模块、数量关系、资料分析、常识理论。
- 分类按各模块继续细分，例如言语下有成语、三字词、诗词、关联词、实词；资料分析下有增长率、增长量、比重等。
- 常规大题库每组最多 20 条。分组是学习节奏，不是永久完成状态，允许随时重学。
- 单条学习采用一张占主要屏幕空间的大卡片，通过滑动或上一条/下一条逐条学习。
- 成语、三字词和实词默认只显示词语与学习提示，用户点击“查看解析”后再显示释义、详细解析和记忆信息；不要一进入就暴露答案。
- 支持的分类提供“全部查看与搜索”，可按标题和释义查找。大词库目录每次渐进加载 40 条，避免一次渲染数百条。

### 3.2 考试

- 先选择模块和分类，再从该分类题库随机抽取客观选择题。
- 大题库提供 10 题或 20 题选项；不要提供一次考完 888 道等超大组卷选项。
- 题目答错后记录题目、答题次数、错误次数、正确率/错误率和当前错题状态。
- 正常考试与复习中的错题专项考试是两个入口，但使用同一套统计口径。
- 错题专项考试随机抽取最多 20 题，不能把全部错题一次性传入页面和逐题同步写入本地存储，以免大错题集造成卡顿。

### 3.3 复习

- 复习页按“模块 → 分类”形成层级，而不是把所有错题无限纵向平铺。
- 分类层展示答题次数、正确率和当前错题数。
- 用户更关心“具体哪一题错得最多”；详情页优先按错误次数展示高频错题，当前摘要展示前 5 条。
- 可从分类错题直接发起专项考试。
- 统计中的“当前错题”应在后来答对后按既定逻辑消除，但历史错误次数和答题次数仍保留。

## 4. 已确认的数据基线

除非本次任务明确增删内容，否则修改后必须保持以下数量和顺序约束：

| 内容 | 知识条数 | 题目数 | 备注 |
| --- | ---: | ---: | --- |
| 四字成语 | 888 | 888 | 原有 153 + 图片增量 735 |
| 三字词 | 113 | 113 | 来源为用户提供的 `三字词.docx` |
| 关联词 | 10 类关系 | 30 | 每类学习多个关联标志词 |
| 资料分析 | 25 张公式卡 | 50 | 7 个分类，均含推导说明 |
| 全项目当前合计 | 1044 | 1089 | 包含其他基础示例内容 |

### 4.1 成语词库的来源和先后顺序

成语是增量合并，不是后一次资料替换前一次资料：

1. **原有高频词库 153 条必须排在最前面**。它包含 Excel 中的 100 条以及更早资料中去重后保留的成语。
2. `成语800词.zip` 图片资料实际有编号 1—839：
   - 排除 4 个非四字词：`海水不可斗量`、`百思不得其解`、`夏虫不可语冰`、`可望而不可即`；
   - 图片资料内部有 1 个重复标题 `老生常谈`；
   - 与原有词库重合 99 条；
   - 去重后净新增 735 条，排在原有 153 条之后。
3. 图片增量必须保持照片编号升序；去重时保留原有版本，不能覆盖原有释义、ID、顺序和历史统计。
4. 现行严格分组不能跨资料批次混合：
   - 第 1—7 组：每组 20 条原有高频词；
   - 第 8 组：最后 13 条原有高频词；
   - 第 9—44 组：每组 20 条“800词补充”；
   - 第 45 组：最后 15 条“800词补充”。
5. 分组页用“原有高频词库”和“800词补充”标明批次，帮助用户按高频到低频学习。

### 4.2 今后导入资料的默认规则

- 用户说“新增、补充、又加了一份”时，默认执行**增量追加**，不是替换现有词库。
- 默认按规范化后的准确标题去重；标题冲突时保留现有条目，除非用户明确要求覆盖或修订。
- 新资料按其原始可解释顺序追加在对应分类末尾；不要随机重排既有内容。
- 导入前后必须核对数量、重复项、被排除项和新增净值，并把结果同步到 `README.md` 与本文件的数据基线。
- 现有稳定 ID 由标题等信息生成并承载历史统计，不能仅为“更整齐”而重建 ID。
- 原始大体积照片资料可以不进仓库；清洗、复核后的结构化结果需要纳入代码。目前图片成语结果位于 `miniprogram/data/materials/idioms800.js`。

### 4.3 特殊内容建模

- 三字词是言语模块中的独立“三字词”分类，不并入四字成语。
- 关联词不能按“因此”“但是”等单个词逐张学习。应按因果、转折、递进、并列、条件、假设等**逻辑关系**建卡，一张卡学习该关系常见的多个关联标志及辨析。
- 资料分析当前分类：增长率、增长量、比重、平均数、倍数与比值、混合增长、贡献与拉动。
- 数学公式必须使用真实的分子/分母和分数线排版，不能把分式按普通文字顺序挤在一行。每张公式卡尽量包含基础定义、变形或推导过程、适用条件和易错点。

## 5. 界面和交互规则

- 视觉整体采用统一的蓝色系；正确/中性信息使用蓝色和灰蓝色，错误或危险状态可使用克制的红色。
- 设计基线见 `design-system/default/MASTER.md`；实现时还要以当前小程序的实际视觉和用户最新反馈为准。
- 首页、模块页、分类页和分组页都使用接近容器全宽的大卡片。不能只把首页改大，而下一级仍保留窄小卡片。
- 学习卡是页面视觉主体，左右留白适中，不能出现“两边很窄、中间细高”的卡片。
- 文字、按钮和底部上一条/下一条操作要在视觉上居中、对齐均衡，不能只有按钮外框居中而文字偏移。
- 交互元素要有足够的手机触控面积；信息层级要清楚，避免过长列表。
- 不用 emoji 充当结构性图标；同一层级的图形、圆角、阴影、间距和颜色必须一致。
- 公式统一通过 `components/math-formula` 等数学结构组件显示，新增公式时复用该能力。
- 全局视觉采用 `design-system/default/MASTER.md` 定义的 “Calm Focus” 体系：冷白背景、克制蓝色主色、青绿色辅助、语义化状态色、统一 8rpx 间距基线和线性 TabBar 图标；正确/错误/选中状态不得只依赖颜色。

## 6. 关键代码位置

- `miniprogram/data/content.js`：模块、分类、基础内容、合并后的知识/题目、分组逻辑。
- `miniprogram/data/materials/idioms.js`：成语主数据、稳定 ID 和旧 ID 别名迁移。
- `miniprogram/data/materials/idioms800.js`：图片“800词”清洗后的增量数据。
- `miniprogram/data/materials/threeCharacterWords.js`：三字词数据。
- `miniprogram/data/materials/connectives.js`：按逻辑关系组织的关联词。
- `miniprogram/data/materials/dataAnalysis.js`：资料分析公式、推导和题目。
- `miniprogram/utils/storage.js`：本地答题统计、学习进度及一次性迁移。
- `miniprogram/utils/cloudSync.js`：微信登录、令牌管理、拉取、批量上传和离线重试。
- `miniprogram/config/cloud.js`：生产云端开关和 HTTPS API 域名，不得放任何密钥。
- `miniprogram/pages/group`：分类分组与批次展示。
- `miniprogram/pages/learn`：逐卡学习和解析展开。
- `miniprogram/pages/catalog`：全部查看和搜索。
- `miniprogram/pages/exam`：正常考试和错题考试。
- `miniprogram/pages/review*`：模块、分类和高频错题复习。
- `miniprogram/components/math-formula`：数学分式等结构化公式显示。
- `project.config.json`：微信开发者工具项目配置和 AppID。
- `server/src`：自建云端同步 API、微信 code 换 OpenID、会话签名和 SQLite 数据访问。
- `server/deploy`：Nginx 与 systemd 部署模板。
- `server/README.md`：服务器、域名、证书、环境变量和微信后台配置说明。

## 7. ID、本地统计和迁移约束

- 用户的学习进度、题目统计和错题记录都依赖稳定的知识 ID、题目 ID、分类 ID 和分组索引。
- 追加内容时不得改变现有条目的 ID；同名合并优先映射到现有 ID。
- 不得在没有明确必要和说明时清空 `wx` 本地存储或让历史统计失效。
- 如果不可避免地改变 ID、分类或分组边界，必须在 `miniprogram/utils/storage.js` 增加可重复执行且只生效一次的迁移，并验证旧数据与新数据两种状态。
- 当前已有：关联词记录迁移、成语增量合并 ID 迁移、2026-08-21 成语批次分组进度迁移。后续不能误删。
- 当前批次分组迁移保留原第 8 组 13 条的旧进度，并清理曾因分组位移产生的后续临时进度，避免出现虚假的已学习状态。
- 每次答题除更新本地聚合统计外，还会写入带唯一 `eventId` 的待同步事件；服务端保存事件 ID 做幂等去重。不得改回“整份统计直接覆盖”导致多设备丢计数。
- 云端首次初始化只在该用户尚无云端状态时导入本机基线；已有云端状态的新设备应以云端为准，再同步本机新产生的事件。
- “清空答题记录”同时产生云端重置信号；不能只清本机后又被云端旧数据恢复。学习进度不随答题统计一起删除。

## 8. 修改后的最低验证标准

每次交付前按改动风险执行验证，至少包括：

1. `git diff --check`。
2. 对所有 `miniprogram/**/*.js` 执行 `node --check`。
3. 云同步改动还必须执行：
   - `node --test tests/cloud-sync.test.js tests/storage-sync.test.js`；
   - 在 `server/` 执行 `npm test`；
   - 验证首次导入、重复事件不重复计数、用户隔离、两次答对消除当前错题、云端清空和进度保留。
4. 数据改动必须验证：
   - 知识 ID 唯一、题目 ID 唯一；
   - 每道题关联的知识存在；
   - 正确选项存在于选项列表；
   - 每个分类分组展平后与原分类内容顺序完全一致，无遗漏、无重复；
   - 成语数量仍为 888，除非任务明确要求新增或删除；
   - 增量导入前的旧内容前缀、ID、标题、释义和顺序保持不变。
5. 考试题量要覆盖 0、1—10、11—20、21—100、100 以上等边界；不能生成 0 道或超过页面可承载数量的试卷。
6. 有界面或路由改动时，用微信开发者工具编译并检查手机宽度下的卡片、文字居中、滚动、搜索、解析展开、考试提交和复习入口。
7. 上线云端前还要验证 `/health`、HTTPS 证书、微信合法域名、真实 `wx.login`、首次同步、断网继续学习和恢复网络补传。
8. 交付前再次查看 `git status`，确认没有遗漏本次必要文件，也没有误提交原始大文件、私有配置或无关文件。

## 9. GitHub 与跨电脑协作流程

### 在另一台电脑继续开发

1. 安装 Git、Codex 和微信开发者工具。
2. 克隆：`git clone https://github.com/qq808shd/zhilian-mini-program.git`。
3. 在 Codex 中打开克隆后的仓库根目录；Codex 应先读取本文件获取项目上下文。
4. 微信开发者工具导入该根目录，核对 AppID `wx01e13d3df486356c`。
5. 每次开始工作先执行 `git pull origin main`，避免在旧版本上继续改。

### 提交和推送

- 默认在 `main` 分支工作，远端名为 `origin`。
- 先验证，再创建清晰的 Git 提交，最后执行普通的 `git push origin main`。
- **只允许正常 HTTPS Git 推送**。不要改用代理、SSH、镜像、临时补丁包或其他替代方式。正常推送失败时停止并如实报告错误，等待用户处理网络或授权。
- 不提交令牌、账号密码、私有配置、逐字聊天记录、Codex 本机记忆或无关临时文件。
- 每个被推送的项目提交都必须包含本文件的同步更新，具体规则见第 0 节。

## 10. 对话决策摘要

以下是截至 2026-09-03 已确认、会影响后续实现的主要决定：

1. 小程序以个人背诵和客观题训练为主，核心是能学、能考、能定位高频错题。
2. 学习内容先按模块和分类进入，再按每组约 20 条学习；学习主体是一张可滑动的大卡。
3. 考试按分类随机抽题，错误需要累计统计；复习页先看分类表现，再进入查看具体高频错题并专项再考。
4. 全部层级统一采用大卡和蓝色视觉，不接受只改首页、下一级仍是窄小卡片的局部修补。
5. 成语、三字词和实词先隐藏释义，点击查看解析后展示。
6. 关联词改为按逻辑关系学习一组标志词，不按单个词机械背诵。
7. 新增资料分析模块，公式要有推导，并用真正的分数线结构显示。
8. 成语和三字词支持“全部查看与搜索”；大词库使用渐进加载。
9. 用户提供的新资料默认视为增量。后传的 800 词图片不能替换前面的原有成语。
10. 成语学习顺序按资料优先级固定：原有 153 条高频词在前，图片净新增 735 条在后，而且分组不得跨批次混合。
11. 为支持本人和身边朋友使用，采用阿里云北京地域轻量应用服务器同步用户学习数据；题库继续留在小程序，不让低配服务器承担静态资料请求。
12. 云同步坚持本地优先、离线可用、批量上传和事件幂等；不因服务器故障中断学习。
13. 用户身份使用微信 OpenID，不建设密码账号体系，不收集非必要个人资料。
14. GitHub 只保存项目文件，不自动保存聊天记忆；因此用本文件维护可审查、可版本化的长期上下文。
15. 2026-09-03 起改用新注册的小程序 AppID `wx01e13d3df486356c`；旧 AppID 下的本地学习记录无需迁移，新小程序按全新用户数据空间启用。

## 11. 变更记录

- **2026-09-04**：完成全项目 UI / UX 视觉升级，建立 Calm Focus Design System，统一学习、考试、解析、复习、目录和详情页面的颜色、排版、间距、卡片、按钮、状态与 TabBar 图标；根据真实设备预览和 Image 2 复审进一步提高搜索、进度、统计及状态文字的对比度，并统一覆盖微信原生普通按钮注入的固定宽度，保证模块、分类、分组、考试和复习列表卡片接近容器全宽；学习页使用分段式进度避免动态内联样式提示，业务逻辑、数据结构和页面跳转保持不变。
- **2026-09-03**：正式名称确认为“知练个人笔记”；备案域名 `api.dabaommz.cloud`、AppSecret、HTTPS 证书和微信 `request` 合法域名已配置，Nginx 与 `zhilian-api` 已启用，生产云同步开关已打开。真实微信登录首次同步验证通过：10 次作答（1 对、9 错）、9 道当前错题和 5 条学习进度成功入云；重复编译后计数保持不变，事件幂等有效。当前 90 天证书截至北京时间 2026-12-02 07:59:59，需在到期前换新。
- **2026-09-03**：项目切换至新注册的小程序 AppID `wx01e13d3df486356c`，同步更新微信开发者工具配置、服务端环境示例、阿里云后端 AppID 和长期文档；旧 AppID 的本地记录明确不迁移，生产云同步仍等待新 AppSecret、备案 HTTPS 域名和微信合法域名。
- **2026-08-25**：生产目标由境外腾讯云服务器迁移为阿里云北京地域轻量应用服务器；建立 SSH 别名 `zhilian-aliyun`，完成 Ubuntu 24.04、隔离 Node.js 24.19、Nginx、低权限运行账户、私密环境文件、数据目录和 systemd 前置部署，远端后端测试 2/2 通过。生产服务仍等待备案 HTTPS 域名与 AppSecret，当前保持禁用和停止。
- **2026-08-22**：复用既有 SSH 别名 `cloud` 接入腾讯云并完成知练隔离 Node.js 24、低权限服务用户、部署目录和云端接口测试；明确不得修改同机 CPA 的 Tailscale Funnel，生产启动仍等待 AppSecret 与备案 HTTPS 域名。
- **2026-08-22**：加入腾讯云自建服务器同步方案。新增 Node.js 24 + SQLite 轻量 API、微信登录、签名会话、用户隔离、首次本机基线导入、答题事件幂等同步、学习进度同步、云端清空、离线重试、Nginx/systemd 模板和测试。题库仍保留本地，生产连接等待 HTTPS 域名与服务器部署后启用。
- **2026-08-21**：新增仓库级 `AGENTS.md`，汇总产品、数据、视觉、架构、验证及跨电脑协作规则；确定今后每次项目提交和推送都必须同步更新本文件。
- **2026-08-21 / `987061a`**：成语按资料批次严格分组，原有 153 条位于第 1—8 组，图片新增 735 条位于第 9—45 组，并加入学习进度迁移。
- **2026-08-12 / `7111d20`**：从“成语800词”图片资料净增 735 条，成语总数达到 888 条，目录改为渐进加载，超大题库限制为 10/20 题。
- **2026-08-08 / `466b377`、`e75d186`**：合并并保留原有成语词库，导入更新后的 Excel 成语和 Word 三字词资料，确立增量而非替换原则。
- **2026-08-07 / `d248cbd`**：成语和实词改为点击后显示释义。
- **2026-07-30 / `4aadce9`、`26301b9`**：加入资料分析公式学习模块，并将公式升级为标准数学结构排版。
