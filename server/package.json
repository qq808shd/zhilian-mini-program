{
  "name": "zhilian-sync-api",
  "version": "1.0.0",
  "private": true,
  "description": "知练小程序的微信登录与学习记录同步服务",
  "type": "commonjs",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "start:env": "node --env-file=.env src/index.js",
    "test": "node --test"
  }
}
