<view class="page account-page"><view class="page-header"><text class="eyebrow">学习数据</text><text class="page-title">安心积累，接着学</text></view>
  <view class="card sync-detail"><image class="sync-symbol" src="/assets/account/cloud.svg" /><text class="section-title">{{sync.title}}</text><text class="page-subtitle">{{sync.description}}</text><view class="sync-facts"><view><text>学习数据同步</text><text>{{sync.authorized ? '已开启' : '未开启'}}</text></view><view><text>上次成功同步</text><text>{{sync.lastSynced}}</text></view><view><text>待同步作答</text><text>{{sync.pendingCount}} 条</text></view><view wx:if="{{sync.resetPending}}"><text>答题清空指令</text><text>等待同步</text></view></view></view>
  <text wx:if="{{draft}}" class="draft-note section">当前为开发预览，协议与主体信息待补齐。正式发布前需要重新确认完整协议。</text>
  <view class="section stack"><button class="primary-button" loading="{{busy || sync.state === 'syncing'}}" disabled="{{busy || sync.state === 'syncing'}}" bindtap="onSync">{{sync.authorized ? '立即同步' : '开启学习数据同步'}}</button><button wx:if="{{sync.authorized}}" class="secondary-button" bindtap="onStop">停止同步</button></view>
  <view class="section"><text class="section-title">会同步哪些内容？</text><text class="page-subtitle">通过微信身份识别你的学习记录，同步分组进度、答题统计和错题状态。题库仍保存在小程序内，头像昵称仅保存在此设备。</text><text class="page-subtitle">网络中断时可以继续学习；重新开启同步后，未上传的记录会继续处理。停止同步不等于删除云端数据。</text></view>
</view>
